<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.HistoryMapper">

    <select id="selForHistorySearch" resultType="HistorySearch">
        SELECT * FROM historysearch WHERE uid = #{uid}
    </select>

    <insert id="insHistory" >
        INSERT INTO historysearch(uid,historySearchName) VALUES (#{uid},#{key})
    </insert>

</mapper>